language: generic
sudo: required
services:
  - docker

before_install:
  - docker build -t karreem/docker-react -f Dockerfile.dev .
  
script:
  - docker run -e CI=true karreem/docker-react npm run test

